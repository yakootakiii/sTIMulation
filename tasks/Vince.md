# Vince — Deployment, DevOps & Flexible Lead

Priority tasks to implement now:
- [ ] Verify `Dockerfile` and `docker-compose.yml` build locally
- [ ] Harden Docker image (use non-root user if possible)
- [ ] Finalize GitHub Actions CI to run tests and lint
- [ ] Prepare production deployment manifest (eg. simple `docker-compose.prod.yml`)
